# メイン環境（app.py と Qwen3-TTS 用）に入れるもの。
# torch は CUDA 版を別コマンドで入れるため、ここには書かない（README参照）。
qwen-tts
soundfile
gradio
tkinterdnd2   # 「保存した声」タブのファイルドラッグ&ドロップ
librosa       # 後処理（ピッチシフト・タイムストレッチ）
pypdf         # ファイル読み込み（PDF）
python-docx   # ファイル読み込み（Word .docx）
